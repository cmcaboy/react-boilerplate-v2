import React from 'react';
import PropTypes from 'prop-types';

const propTypes = {};

const defaultProps = {};

export default class ResumeClicker extends React.Component {
    render() {
        return (
            <div>
                <p>This is a resume clicker</p>
            </div>
        );
    }
}

 ResumeClicker.propTypes = propTypes;
 ResumeClicker.defaultProps = defaultProps;